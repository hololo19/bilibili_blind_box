# 🚀 快速开始 - Docker部署

## 最简单的部署方法

### 第一步：上传文件到服务器

```bash
# 在本地执行，上传项目到服务器
scp -r /path/to/project user@your-server-ip:/home/blind-box/
```

### 第二步：连接服务器

```bash
ssh user@your-server-ip
```

### 第三步：进入目录并部署

```bash
cd /home/blind-box/
chmod +x deploy.sh
./deploy.sh
```

### 第四步：访问网页

在浏览器中打开：`http://your-server-ip:5000`

---

## 📺 在OBS中配置

1. 添加 → 浏览器源
2. URL: `http://your-server-ip:5000`
3. 宽度: `1920`
4. 高度: `1080`
5. 帧率: `30`

---

## ✅ 完成！

现在可以在直播中实时显示盲盒统计了！

---

**需要帮助？** 查看 [DOCKER_DEPLOY.md](DOCKER_DEPLOY.md)
